package com.example.vocabularybook.util;

public class AppWords {
    public static final String SQL_CREATE_DATABASE = "create table note(id INTEGER PRIMARY KEY AUTOINCREMENT, note VARCHAR(256), time varchar(20))";
}
